function background() {
    document.querySelectorAll('.hideable').forEach(el => el.classList.toggle('hidden'));
  }

function foreground() {
    document.querySelectorAll('.hideable').forEach(el => el.classList.toggle('hidden'));
  }